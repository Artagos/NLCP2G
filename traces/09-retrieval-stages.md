# Retrieval, stage by stage

Eight queries through the pipeline: the seven where the four stages
disagree most about where the right passage sits, plus one
out-of-corpus case. For each, what dense-only, BM25-only, RRF-fused and
reranked returned — and which stage actually fixed it.

The cases are picked from the data rather than by hand, because not
every query is interesting: of the 21 answerable cases considered,
**7** have all four stages putting the right chunk at the
same rank, with nothing to show. Reporting that is part of an honest
before/after — the stages earn their keep on the hard queries, not on
all of them.

Corpus: 152 chunks from 21 documents. Retrieve 30 dense + 30 lexical, fuse with RRF (k=60), rerank the top 20.

`✓` marks a chunk in the case's golden set. The verdict line is derived
from the rank of the first golden chunk at each stage, not written by
hand — regenerate with `python -m scripts.demo_rag` and it re-derives.

Generated by `scripts/demo_rag.py`.

---

## paraphrase — paraphrase-too-slow

> my program works but the judge says it took too long, what now

*the target sections never use the words "took too long"; they say TLE, complexity, and constant factor*

Wanted: `debugging-tle-wa :: Finding the slow part`, `debugging-tle-wa :: Diagnose before you edit`

**dense only**

1. ✓ `debugging-tle-wa` :: Diagnose before you edit
2.   `debugging-tle-wa` :: Debugging output that does not break the submission
3.   `debugging-tle-wa` :: When it works locally and fails on the judge

**BM25 only**

1.   `debugging-tle-wa` :: When it works locally and fails on the judge
2.   `io-and-runtime-errors` :: Fast input and output
3.   `complexity-and-big-o` :: What big-O actually measures

**RRF fused**

1.   `debugging-tle-wa` :: When it works locally and fails on the judge
2.   `io-and-runtime-errors` :: Fast input and output
3.   `debugging-tle-wa` :: Stress testing, the reliable technique

**reranked**

1.   `io-and-runtime-errors` :: The verdicts and what they usually mean
2. ✓ `debugging-tle-wa` :: Diagnose before you edit
3. ✓ `debugging-tle-wa` :: Finding the slow part

**Verdict.** BM25 missed it entirely; vector search found it at rank 1; RRF pushed it down to 12; reranking lifted it to 2.

---

## paraphrase — paraphrase-estimate-speed

> how can I tell if my idea will be fast enough before I write it

*asks about a table it shares no vocabulary with*

Wanted: `complexity-and-big-o :: How many operations fit in a second`

**dense only**

1. ✓ `complexity-and-big-o` :: How many operations fit in a second
2.   `debugging-tle-wa` :: Finding the slow part
3.   `io-and-runtime-errors` :: Reading the constraints before writing anything

**BM25 only**

1.   `debugging-tle-wa` :: Stress testing, the reliable technique
2.   `io-and-runtime-errors` :: Fast input and output
3.   `arrays-and-prefix-sums` :: Two dimensions

**RRF fused**

1.   `debugging-tle-wa` :: Stress testing, the reliable technique
2.   `io-and-runtime-errors` :: Reading the constraints before writing anything
3.   `io-and-runtime-errors` :: Fast input and output

**reranked**

1.   `io-and-runtime-errors` :: Reading the constraints before writing anything
2. ✓ `complexity-and-big-o` :: How many operations fit in a second
3.   `debugging-tle-wa` :: Finding the slow part

**Verdict.** BM25 missed it entirely; vector search found it at rank 1; RRF pushed it down to 10; reranking lifted it to 2.

---

## ambiguous — ambiguous-memory-usage

> how much memory will this use

*no subject; "this" refers to something the corpus cannot see*

Wanted: `complexity-and-big-o :: Space complexity counts too`

**dense only**

1. ✓ `complexity-and-big-o` :: Space complexity counts too
2.   `complexity-and-big-o` :: How many operations fit in a second
3.   `dynamic-programming-patterns` :: Reducing the memory

**BM25 only**

1.   `recursion-and-backtracking` :: The call stack, and how it runs out
2.   `fenwick-tree` :: Fenwick versus segment tree
3.   `complexity-and-big-o` :: The growth rates you will meet

**RRF fused**

1.   `recursion-and-backtracking` :: The call stack, and how it runs out
2.   `complexity-and-big-o` :: How many operations fit in a second
3.   `dynamic-programming-patterns` :: Reducing the memory

**reranked**

1.   `recursion-and-backtracking` :: The call stack, and how it runs out
2.   `dynamic-programming-patterns` :: Reducing the memory
3.   `graph-traversal-bfs-dfs` :: Representing a graph

**Verdict.** BM25 missed it entirely; vector search found it at rank 1; RRF pushed it down to 12; reranking lifted it to 5.

---

## multi-hop — multihop-passes-sample-fails-later

> it passes the sample but fails on a later test, what should I check

*two documents, one about edge cases and one about the arithmetic*

Wanted: `debugging-tle-wa :: Edge cases worth checking every time`, `io-and-runtime-errors :: Integer overflow`

**dense only**

1.   `debugging-tle-wa` :: Reading your own code for the usual suspects
2.   `debugging-tle-wa` :: Diagnose before you edit
3.   `debugging-tle-wa` :: Stress testing, the reliable technique

**BM25 only**

1.   `graph-traversal-bfs-dfs` :: Mistakes worth knowing
2.   `debugging-tle-wa` :: Reading your own code for the usual suspects
3.   `debugging-tle-wa` :: Diagnose before you edit

**RRF fused**

1.   `debugging-tle-wa` :: Reading your own code for the usual suspects
2.   `debugging-tle-wa` :: Diagnose before you edit
3.   `debugging-tle-wa` :: Stress testing, the reliable technique

**reranked**

1.   `debugging-tle-wa` :: Diagnose before you edit
2.   `debugging-tle-wa` :: When it works locally and fails on the judge
3. ✓ `debugging-tle-wa` :: Edge cases worth checking every time

**Verdict.** BM25 missed it entirely; vector search found it at rank 4; RRF pushed it down to 12; reranking lifted it to 3.

---

## ambiguous — ambiguous-how-trees-work

> how do trees work

*could mean the graph structure, a binary search tree, or a segment tree; three documents contain "tree" in their title*

Wanted: `trees-and-lca :: What makes a tree special`

**dense only**

1. ✓ `trees-and-lca` :: What makes a tree special
2.   `segment-tree` :: The structure
3.   `trees-and-lca` :: Rooting, depth and subtree sizes

**BM25 only**

1.   `complexity-and-big-o` :: The growth rates you will meet
2.   `trees-and-lca` :: Rooting, depth and subtree sizes
3.   `complexity-and-big-o` :: What big-O actually measures

**RRF fused**

1.   `trees-and-lca` :: Rooting, depth and subtree sizes
2. ✓ `trees-and-lca` :: What makes a tree special
3.   `dsu-union-find` :: Union by rank or by size

**reranked**

1. ✓ `trees-and-lca` :: What makes a tree special
2.   `trees-and-lca` :: Rooting, depth and subtree sizes
3.   `trees-and-lca` :: Common tree patterns

**Verdict.** Both arms found it (dense 1, BM25 13); RRF pushed it down to 2; reranking lifted it to 1.

---

## paraphrase — paraphrase-negative-result

> my answer comes out negative when it should not

*a symptom, not a topic; the answer is in a section about the % operator*

Wanted: `modular-arithmetic :: Subtraction and the negative remainder`

**dense only**

1. ✓ `modular-arithmetic` :: Subtraction and the negative remainder
2.   `shortest-paths` :: Why Dijkstra needs non-negative weights
3.   `modular-arithmetic` :: Mistakes worth knowing

**BM25 only**

1.   `shortest-paths` :: Why Dijkstra needs non-negative weights
2.   `stl-containers` :: set and map: ordered, tree-backed
3. ✓ `modular-arithmetic` :: Subtraction and the negative remainder

**RRF fused**

1.   `shortest-paths` :: Why Dijkstra needs non-negative weights
2. ✓ `modular-arithmetic` :: Subtraction and the negative remainder
3.   `debugging-tle-wa` :: Stress testing, the reliable technique

**reranked**

1. ✓ `modular-arithmetic` :: Subtraction and the negative remainder
2.   `modular-arithmetic` :: Mistakes worth knowing
3.   `debugging-tle-wa` :: Reading your own code for the usual suspects

**Verdict.** Both arms found it (dense 1, BM25 3); RRF pushed it down to 2; reranking lifted it to 1.

---

## multi-hop — multihop-changing-range-sums

> how do I answer range sum questions when the array keeps changing

*needs the limitation from one document and the replacement from another*

Wanted: `arrays-and-prefix-sums :: When prefix sums do not apply`, `fenwick-tree :: What problem it solves`

**dense only**

1.   `segment-tree` :: The structure
2.   `arrays-and-prefix-sums` :: Difference arrays: range update, point query
3. ✓ `arrays-and-prefix-sums` :: When prefix sums do not apply

**BM25 only**

1.   `arrays-and-prefix-sums` :: What a prefix sum is
2.   `segment-tree` :: The structure
3. ✓ `arrays-and-prefix-sums` :: When prefix sums do not apply

**RRF fused**

1. ✓ `arrays-and-prefix-sums` :: When prefix sums do not apply
2.   `arrays-and-prefix-sums` :: Difference arrays: range update, point query
3.   `arrays-and-prefix-sums` :: What a prefix sum is

**reranked**

1. ✓ `fenwick-tree` :: What problem it solves
2.   `segment-tree` :: What problem it solves
3.   `fenwick-tree` :: Fenwick versus segment tree

**Verdict.** Both arms found it (dense 3, BM25 3); RRF promoted it to 1; reranking left it at 1.

---

## out-of-corpus — ooc-language-not-covered

> how do I read input quickly in Python

*the nastiest of the three: the corpus has a section on exactly this topic for a different language, so retrieval WILL return confident, wrong-language passages and the answerer has to notice*

Wanted: **nothing** — this question is not answerable from the corpus.

**dense only**

1.   `io-and-runtime-errors` :: Fast input and output
2.   `io-and-runtime-errors` :: Reading the constraints before writing anything
3.   `io-and-runtime-errors` :: Reading mixed lines and tokens

**BM25 only**

1.   `debugging-tle-wa` :: Stress testing, the reliable technique
2.   `io-and-runtime-errors` :: Fast input and output
3.   `modular-arithmetic` :: Subtraction and the negative remainder

**RRF fused**

1.   `io-and-runtime-errors` :: Fast input and output
2.   `io-and-runtime-errors` :: Reading mixed lines and tokens
3.   `debugging-tle-wa` :: Stress testing, the reliable technique

**reranked**

1.   `io-and-runtime-errors` :: Fast input and output
2.   `debugging-tle-wa` :: Finding the slow part
3.   `io-and-runtime-errors` :: Reading the constraints before writing anything

**Verdict.** No golden chunk exists. Every stage returns confident, on-topic, wrong-subject passages; catching that is the answerer's job, not the retriever's.

---
